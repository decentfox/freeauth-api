# FreeAuth FastAPI Example

## Setup environment

1. Before using the app, run `make setup` to prep environment, install dependencies, and initial project.

2. Run `make dev` to start a development server.
